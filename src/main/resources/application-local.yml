spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/licensed_tennis_matches
    username: local_dev
    password: imgarena
  flyway:
    user: local_dev
    password: imgarena
    schemas: licensed_tennis_matches

server:
  error:
    include-message: always
    include-binding-errors: always